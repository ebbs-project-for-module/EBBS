@model IEnumerable<EBBS.Data.SecurityQuestion>

@{
    ViewBag.Title = "SecurityQuestions";
}

<h2>SecurityQuestions</h2>

<p>
    @Html.ActionLink("Create New", "Create")
</p>
<table class="table">
    <tr>
        <th>
            @Html.DisplayNameFor(model => model.question)
        </th>
        <th></th>
    </tr>

@foreach (var item in Model) {
    <tr>
        <td>
            @Html.DisplayFor(modelItem => item.question)
        </td>
        <td>
            @Html.ActionLink("Edit", "Edit", new { id=item.qId }) |
            @Html.ActionLink("Details", "Details", new { id=item.qId }) |
            @Html.ActionLink("Delete", "Delete", new { id=item.qId })
        </td>
    </tr>
}

</table>
